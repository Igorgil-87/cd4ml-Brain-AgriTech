apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: interface-claim0
  name: interface-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
