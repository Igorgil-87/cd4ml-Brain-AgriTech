apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.rendered.yml --out k8s-out --verbose
    kompose.version: 1.38.0 (HEAD)
  labels:
    io.kompose.service: dagster-webserver
  name: dagster-webserver
spec:
  ports:
    - name: "3005"
      port: 3005
      targetPort: 3000
  selector:
    io.kompose.service: dagster-webserver
